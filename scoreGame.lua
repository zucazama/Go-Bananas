scoreGame = {}



function  score.new(text)
    -- body
end

do

    text = love.graphics.newText(font, "SCORE")
    positionX = (text:getWidth() + margens) - _G.width
    positionY = 
end


scoreGame[1] = {
    ['position'] = {
        ['x'] = 
    }
    ['text'] = love.graphics.newText(font, "SCORE")
}